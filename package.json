{
  "name": "eu-barbeiro-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "web",
    "mobile"
  ],
  "scripts": {
    "dev:web": "docker compose up -d web && docker compose logs -f web",
    "dev:backend": "docker compose up -d backend db redis && docker compose logs -f backend",
    "dev:mobile": "npm -w mobile run start"
  }
}
